{
  "name": "@groton/email-new-drive-files",
  "description": "Google Drive Add-on to monitor folders for new files and email notifications",
  "version": "0.1.3",
  "license": "GPL-3.0",
  "author": {
    "name": "Seth Battis",
    "url": "https://github.com/groton-school"
  },
  "dependencies": {
    "@battis/gas-lighter": "^0.4.4"
  },
  "devDependencies": {
    "@battis/prettier-config": "^1.3.0",
    "clasp": "^1.0.0",
    "npm-run-all": "^4.1.5"
  },
  "scripts": {
    "build": "webpack",
    "deploy": "run-s deploy:*",
    "deploy:build": "npm run build",
    "deploy:push": "clasp push --force"
  },
  "prettier": "@battis/prettier-config",
  "eslintConfig": {
    "extends": "@battis/gas-lighter/.eslintrc.json"
  }
}
